// Load global HTMLElementEventMap augmentation for cropper events (addEventListener typing).
import './cropper/types/dom-events'

import VuePictureCropper from './cropper/index.vue'

export default VuePictureCropper

export * from './cropper/types'
export * from './hooks/useCropper'
